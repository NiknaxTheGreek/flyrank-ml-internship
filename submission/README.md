# submission/

The submission pointer is **`paper_url.txt`**.

It contains exactly one line: the direct URL of the deployed capstone research paper.
The repository URL is the capstone submission; reviewers use `paper_url.txt` to find the deployed paper.

`README.md` is documentation only and is not part of the submission payload.
